Pons-style test files (`sequence score` per line, 1-based columns).

Copied from the public Connect-4 test sets used by several independent solvers
(e.g. WannesMalfait’s port of the gamesolver.org protocol). The scores are
game-theoretic facts, not code.
