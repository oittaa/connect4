# Native CLI only. WASM (wasm32-unknown-unknown) does not match this cfg, so
# ./scripts/build-wasm.sh and wasm32 clippy stay portable. We do not ship
# c4solver; target-cpu=native is for local/CI x86_64 hosts.
[target.'cfg(target_arch = "x86_64")']
rustflags = ["-C", "target-cpu=native"]
